"""SideCraft Side-Chain Module (S_phi) — wrapper-layer package.

Leakage-safe, one-step, global-coordinate side-chain generation that reads/writes the
shared per-residue representation h_res. All components live here; the Protenix
and PXDesign submodules are reused, never edited.
"""
